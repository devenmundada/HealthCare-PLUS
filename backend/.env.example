# Copy this to .env and fill in your values
NODE_ENV=development
PORT=3001

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=your_db_user
DB_PASSWORD=harsh@postgres
DB_NAME=healthcare_plus

JWT_SECRET=your_jwt_secret

# Free at https://console.groq.com/keys — used to power the AI chat assistant
# and symptom triage (Llama 3.3 70B, no cost on the free tier).
GROQ_API_KEY=your_groq_api_key